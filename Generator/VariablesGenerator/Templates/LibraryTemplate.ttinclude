<#@ assembly name="System.Core" #>
<#@ import namespace="System.Linq" #>
<#@ import namespace="System.Text" #>
<#@ import namespace="System.Collections.Generic" #>

<#@ assembly name="VariablesGenerator.dll" #>

<#+
void GenerateIncludeAll(Dictionary<string, GlobalVariable> variables)
{
#>
// Copyright Out-of-the-Box Plugins 2018-2021. All Rights Reserved.

// GENERATED FILE DO NOT MODIFY DIRECTLY

#pragma once

<#+
    for (int i = 0; i < variables.Count; i++)
	{
#>
#include "VariablesSystem/Generated/Global/Global<#=variables.Keys.ElementAt(i)#>.h"
#include "VariablesSystem/Generated/Instanced/Instanced<#=variables.Keys.ElementAt(i)#>.h"
<#+
	}
}
#>

<#+
void GenerateBlueprintLibraryH(Dictionary<string, GlobalVariable> variables)
{
#>
// Copyright Out-of-the-Box Plugins 2018-2021. All Rights Reserved.

// GENERATED FILE DO NOT MODIFY DIRECTLY

#pragma once

#include "Kismet/BlueprintFunctionLibrary.h"

#include "IncludeAll.h"

#include "GenericVariablesBPLibrary.generated.h"

UCLASS(meta = (BlueprintThreadSafe))
class VARIABLESSYSTEM_API UGenericVariablesBPLibrary : public UBlueprintFunctionLibrary
{
    GENERATED_BODY()

public:
<#+
    for (int i = 0; i < variables.Count; i++)
    {
#>
    UFUNCTION(BlueprintPure, meta = (DisplayName = "Get Global <#=variables.Keys.ElementAt(i)#> Variable (Thread Safe)"), Category = "VariablesSystem")
    static <#=variables.Values.ElementAt(i).globalVariable.valueType#> GetGlobal<#=variables.Keys.ElementAt(i)#>Safe(UGlobal<#=variables.Keys.ElementAt(i)#>* Variable);

    UFUNCTION(BlueprintPure, meta = (DisplayName = "Get Instanced <#=variables.Keys.ElementAt(i)#> Variable (Thread Safe)"), Category = "VariablesSystem")
    static <#=variables.Values.ElementAt(i).globalVariable.valueType#> GetInstanced<#=variables.Keys.ElementAt(i)#>Safe(UInstanced<#=variables.Keys.ElementAt(i)#>* Variable, UObject* Owner);

<#+
	}
#>
};
<#+
}
#>

<#+
void GenerateBlueprintLibraryCPP(Dictionary<string, GlobalVariable> variables)
{
#>
// Copyright Out-of-the-Box Plugins 2018-2021. All Rights Reserved.

// GENERATED FILE DO NOT MODIFY DIRECTLY

#include "GenericVariablesBPLibrary.h"

<#+
    for (int i = 0; i < variables.Count; i++)
	{
#>
/* STATIC */ <#=variables.Values.ElementAt(i).globalVariable.valueType#> UGenericVariablesBPLibrary::GetGlobal<#=variables.Keys.ElementAt(i)#>Safe(UGlobal<#=variables.Keys.ElementAt(i)#>* Variable)
{
    return UGlobal<#=variables.Keys.ElementAt(i)#>::GetGlobal<#=variables.Keys.ElementAt(i)#>Value(Variable);
}

/* STATIC */ <#=variables.Values.ElementAt(i).globalVariable.valueType#> UGenericVariablesBPLibrary::GetInstanced<#=variables.Keys.ElementAt(i)#>Safe(UInstanced<#=variables.Keys.ElementAt(i)#>* Variable, UObject* Owner)
{
    return UInstanced<#=variables.Keys.ElementAt(i)#>::GetInstanced<#=variables.Keys.ElementAt(i)#>Value(Owner, Variable);
}

<#+
	}
}
#>